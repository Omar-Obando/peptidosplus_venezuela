---
// Alias: /tienda/producto/[slug] → producto-{slug}.html (diseño 100% original)
const { slug } = Astro.params;
if (!slug) return Astro.redirect('/store.html', 308);
return Astro.redirect(`/producto-${slug}.html`, 308);
---
